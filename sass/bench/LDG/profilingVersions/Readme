Profiling versions.

1. 
Try to profile LDG. Can run. But clock result is wired [May be negative numbers]
When use CUDA-GDB, we can find the error at the LDG line:
CUDA Exception: Warp Illegal Instruction
Also, the clock is different across runs.


2.
Successfully profile "--:-:1:-:2     LDG.E tmp, [input_0];".
It takes 3 clock per instruction for reading 4 bytes from the global memory.

同样的code，用cuda-gdb跑，clock数目就有正有负，很奇怪。
我的猜测是，clock是一个会取mod的值，不是单增的。
如果两个instruction之间间隔很久(e.g., context switch), 那么clock的数字就会很奇怪。




